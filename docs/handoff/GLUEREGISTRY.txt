# GLUE REGISTRY -- GENERATED BY glue.py, DO NOT EDIT
#
# Every word below is RESERVED inside multi-word names in any scope where
# the causing pattern is visible. This is the language's reserved-word list.
#
# Adding a line here is a BREAKING CHANGE for every program that imports the
# module: names that were legal stop being legal. Review accordingly.

## ALWAYS RESERVED (prelude -- in scope everywhere, no escape)

    in           for each (_) in (_)                prelude
    otherwise    if (_) then (_) otherwise (_)      prelude
    then         if (_) then (_)                    prelude
    then         if (_) then (_) otherwise (_)      prelude
    times        repeat (_) times                   prelude

## RESERVED ON IMPORT (only in scopes that import the module)

  collections
    by           sort (_) by (_)                    collections
    of           item (_) of (_)                    collections
    where        filter (_) where (_)               collections

  messaging
    to           send (_) to (_)                    messaging

  numbers
    places       round (_) to (_) places            numbers
    to           round (_) to (_) places            numbers

  text
    on           split (_) on (_)                   text
    with         join (_) with (_)                  text

  time
    seconds      every (_) seconds                  time

## RESERVES NOTHING (all words precede the first hole -- the shape to prefer)

    first of (_)                       anchor «first of»
    last of (_)                        anchor «last of»
    broadcast (_)                      anchor «broadcast»
    average of (_)                     anchor «average of»
    count of (_)                       anchor «count of»
    rounded (_)                        anchor «rounded»
    sum of (_)                         anchor «sum of»
    print (_)                          anchor «print»
    stop                               anchor «stop»
    while (_)                          anchor «while»
    length of (_)                      anchor «length of»
    wait until (_)                     anchor «wait until»

## RESERVED BY A SINGLE PATTERN (respell it and the word is free again)

    by           sort (_) by (_)                    collections
    in           for each (_) in (_)                prelude
    of           item (_) of (_)                    collections
    on           split (_) on (_)                   text
    otherwise    if (_) then (_) otherwise (_)      prelude
    places       round (_) to (_) places            numbers
    seconds      every (_) seconds                  time
    times        repeat (_) times                   prelude
    where        filter (_) where (_)               collections
    with         join (_) with (_)                  text

## POSTFIX PATTERNS (leading hole -- EVERY word is glue, most expensive shape)

    (none)

## SUMMARY

    patterns declared        24
    reserve nothing          12
    distinct reserved words  12
    always reserved          4  (in, otherwise, then, times)
