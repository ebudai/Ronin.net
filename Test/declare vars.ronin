var x = 3.
var this is my var = "12".
var identifier test = some identifier.
var new as int.
var new2 as text.
var dot test = 12.3.
var thingy as int = 6.
var (decon,struc,tion) = (1, 2, 3).
var tuple = (3, 4, thing). // illegal
var uninitialized tuple as (text, int, real). //illegal
var tuple2 as (int, text, Vorgon) = (2, "test", best vorgon)! // illegal!  what is the name of the first element of tuple2?  can't be anonymous
var (tuple3 as int, tuple4 as text, tuple5 as Table Leg).

function test (three as int) stuff (x co-ordinate as decimal, y as Artwork) and then (foo as Foo)
{
	var seven as int.
	var what = 33.
	var yes = "yes".

	resolve to yes.
}

const Fox as datatype = Dog and Cat.