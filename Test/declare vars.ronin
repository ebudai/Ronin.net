var x = 3.
var this is my data = "12".
var identifier test = some identifier.
var new:int.
var new2: text.
var dot test = 12.3.
var thingy :int = 6.
var (decon,struc,tion) = (1, 2, 3).
var tuple = (3, 4, thing).
var uninitialized tuple : (text, int, real).
var tuple2 : (int, text, Vorgon) = (2, "test", best vorgon).