using Ronin.Compiler;

namespace Unit;

[Trait(nameof(Parser), null)]
public class ReactiveScopes
{
    [Fact(DisplayName = "basic")]
    public void Basic()
    {
        // when changing x => y = x;
    }
}
